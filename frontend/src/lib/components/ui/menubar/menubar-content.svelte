<script lang="ts">
	import { Menubar as MenubarPrimitive } from "bits-ui";
	import { cn, type WithoutChild } from "$lib/utils.js";

	let {
		ref = $bindable(null),
		class: className,
		align = "start",
		alignOffset = -4,
		sideOffset = 8,
		children,
		...restProps
	}: WithoutChild<MenubarPrimitive.ContentProps> = $props();
</script>

<MenubarPrimitive.Portal>
	<MenubarPrimitive.Content
		bind:ref
		{align}
		{alignOffset}
		{sideOffset}
		data-slot="menubar-content"
		class={cn(
			"bg-popover text-popover-foreground data-[state=open]:animate-in data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-end-2 data-[side=right]:slide-in-from-start-2 data-[side=top]:slide-in-from-bottom-2 z-50 min-w-[12rem] overflow-hidden rounded-md border p-1 shadow-md",
			className
		)}
		{...restProps}
	>
		{@render children?.()}
	</MenubarPrimitive.Content>
</MenubarPrimitive.Portal>
